# 핵심 정리

- LLM은 최신 정보 부족이라는 한계 존재함.
- LLM은 기업 내부 데이터를 알지 못함.
- 프롬프트에는 토큰 길이 제한 존재함.
- 필요한 정보만 검색하여 전달하는 기술이 RAG임.
- RAG는 Retrieval, Augmented, Generation의 세 단계로 구성됨.
- 인덱싱은 Load → Split → Embed → Store 순서로 수행함.
- 질의 처리는 질문 검색 → 관련 문서 추출 → 문맥 결합 → 답변 생성 과정으로 구성됨.
- 벡터 DB는 의미 기반 검색 수행 방식임.